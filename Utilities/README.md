# Longer (non snippet) albeit useful code.

#### input.min.js
A class that contains most of the keycodes for a british keyboard as well as helper functions that are more readable that document.addEventListener. 
